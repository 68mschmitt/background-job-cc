namespace BackgroundJobCodingChallenge.Constants;

/// Constant enum values to have a readable way to organize the different types of queues we maintain
public enum QueueIds
{
    CustomerActions,
    FinancialSync,
    EmployeeUpload
}
